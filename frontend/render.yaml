# frontend/render.yaml
services:
  - type: web
    name: altitude-bpo-frontend
    env: static
    buildCommand: npm run build
    staticPublishPath: dist  # Vite builds to 'dist', not 'build'
    envVars:
      - key: VITE_API_URL
        value: https://altitude-backend-0j55.onrender.com/api